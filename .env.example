# Secrets for automated tests and workflows.
# Copy to .env and fill in your values. Never commit .env.
#
# All non-secret config (headless, viewport, timeout, log level, etc.)
# belongs in szkrabok.config.toml.

# Login credentials used by automation specs
TEST_USERNAME=your_username
TEST_PASSWORD=your_password

# Add other secrets below (API keys, tokens, etc.)
