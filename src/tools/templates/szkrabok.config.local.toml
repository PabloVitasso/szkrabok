# szkrabok local config — do not commit (gitignored)
# Overrides settings from szkrabok.config.toml for this machine.
# Run: szkrabok doctor detect   to find your Chrome/Chromium path.

[default]
# executablePath = "/path/to/chrome"

# [browser]
# engine = "firefox"
# executable_path = "/path/to/firefox"
# Stealth Firefox handle: invisible_playwright — pip install invisible-playwright && python -m invisible_playwright fetch && python -m invisible_playwright path
